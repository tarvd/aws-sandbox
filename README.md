This repository hosts code to build a data pipeline using AWS: S3, Glue, and Athena.
This is an early work-in-progress.

Steps before running development:
uv run pre-commit install

Steps to run main:
